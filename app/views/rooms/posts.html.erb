<% @rooms.each do |room|%>
  <br/>
  <%= room.id %>
  <%= link_to room.name, room %>
  <%= room.explanation %>
  <%= room.fee %>
  <%= room.address %>
  <img src=  <%= room.room_image %> alt="aaaa">
  <%= room.user_id %>
  <%= link_to "編集", [:edit, room]%>
<% end %>
